;=============================================================================
; GhostSIP — Asterisk logging.
; console  -> docker logs (via supervisord)
; messages + security -> /var/log/asterisk, which the Docker deployment
; mounts to the host (data/asterisk-logs) so host fail2ban can watch for
; SIP brute-force attempts (docs/deployment.md).
;=============================================================================
[general]

[logfiles]
console => notice,warning,error
messages => notice,warning,error
security => security

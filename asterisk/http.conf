;=============================================================================
; GhostSIP — Asterisk built-in HTTP server (carries ARI).
; Bound to localhost ONLY — the webhook receiver runs on the same VPS.
; Never expose 8088 through the firewall.
;=============================================================================
[general]
enabled=yes
bindaddr=127.0.0.1
bindport=8088

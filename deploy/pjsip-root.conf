; GhostSIP: the real PJSIP config is GENERATED by the admin panel into the
; mounted /etc/ghostsip volume. Do not edit either file by hand — use the
; web GUI. entrypoint.sh guarantees the included file exists at start.
#include /etc/ghostsip/pjsip.conf
